import React from 'react';
import CheckBoxGrid from './index';

export default {
  title: 'Organism/CheckBoxGrid',
};

const Template = () => <CheckBoxGrid />;

export const Default = Template.bind({});
