import React from 'react';
import SideNavigation from '.';

export default {
  title: 'Organism/SideNavigation',
};

const Template = () => <SideNavigation />;

export const Default = Template.bind({});
